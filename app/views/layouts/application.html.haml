!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title Platform 3 Solutions
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = javascript_include_tag "vendor/custom.modernizr"
    = csrf_meta_tags
  %body
    %nav.top-bar
      %ul.title-area
        %li.name
          %h1
            %a{href: "/"} Platform 3 Solutions
    .row
      .small-12.columns= yield
    .flashing_messages
      - if notice
        .flash_message.notice_message
          = notice
      - elsif alert
        .flash_message.alert_message
          = alertm