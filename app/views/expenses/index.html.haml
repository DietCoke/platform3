%h1 Expenses

%table
  %thead
    %tr
      %th Description
      %th Amount

  %tbody
    - if @expenses.size > 0
      - @expenses.each do |expense|
        %tr
          %td= expense.desc
          %td= number_to_currency(expense.amount)
    - else
      %tr
        %td
          No Expenses

%br

= link_to 'New Expense', new_expense_path
